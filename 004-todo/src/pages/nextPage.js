import { Link } from 'react-router-dom';

const nextPage = (props) => {
    return (
      <>
      <h2>next page</h2>
        <Link to="/">Click me</Link>
      </>
    )
  }

export default nextPage